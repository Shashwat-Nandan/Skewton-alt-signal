# Credentials and secrets
config.ini
.kite_session.json
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Logs and data
logs/
*.log

# Results (large, regenerated by autoresearch)
results.tsv
results_old*.tsv
results_april.tsv
candidate_params_*.json

# Historical market data cache (regenerable via fetch_historical_data.py)
data_cache/

# Reference docs / scratch (large, not source)
Dynamic_Hedging-Taleb.pdf
shot.png
claude_example.py

# OS
.DS_Store

# Loop-engine runtime memory (regenerable; durable knowledge lives in SKILL.md).
# Tracking it meant a git reset/checkout could clobber the loop's accumulated
# lessons (happened 2026-06-30). SKILL.md (rules/goal/lessons) stays tracked.
state/*/STATE.md
state/*/.STATE.lock
